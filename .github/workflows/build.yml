---
name: Build Fedora

on:
  workflow_dispatch:
  schedule:
    - cron: "0 12 * * 6"
  push:
    branches:
      - main
    paths-ignore:
      - "**.md"
  pull_request:
    branches:
      - main
    paths-ignore:
      - "**.md"

env:
  UPSTREAM_IMAGE: quay.io/fedora-ostree-desktops/silverblue:43
  PREVIOUS_IMAGE: ghcr.io/${{ github.repository_owner }}/${{ github.event.repository.name }}:43

jobs:
  build:
    name: Build and push image
    runs-on: ubuntu-latest
    timeout-minutes: 40
    permissions:
      contents: read
      packages: write

    steps:
      - name: Free up space
        shell: bash
        run: |
          # https://github.com/actions/runner-images/issues/10386
          sudo df -h
          sudo rm -rf /opt/hostedtoolcache /usr/local/.ghcup /opt/ghc /usr/local/lib/android /usr/share/dotnet /usr/local/share/boost
          sudo df -h

      - name: Checkout Git sources
        uses: actions/checkout@v6

      - name: Pull base images
        shell: bash
        run: |
          sudo podman pull '${{ env.UPSTREAM_IMAGE }}'
          sudo podman pull '${{ env.PREVIOUS_IMAGE }}' || { echo "PREVIOUS_IMAGE=" | tee -a "$GITHUB_ENV"; }

      - name: Get upstream version
        id: upstream_version
        shell: bash
        run: |
          version=$(sudo podman image inspect --format '{{ index .Config.Labels "org.opencontainers.image.version" }}' '${{ env.UPSTREAM_IMAGE }}')
          echo "version=$version" | tee -a "$GITHUB_OUTPUT"

      - name: Build image
        shell: bash
        run: |
          sudo buildah build --retry=2 -f Containerfile --build-arg UPSTREAM_IMAGE=${{ env.UPSTREAM_IMAGE }} --tag raw-image .

      - name: Rechunk
        id: rechunk
        uses: hhd-dev/rechunk@v1.2.4
        with:
          rechunk: ghcr.io/hhd-dev/rechunk:v1.2.4
          ref: raw-image
          prev-ref: ${{ env.PREVIOUS_IMAGE }}
          version: ${{ steps.upstream_version.outputs.version }}

      - name: Push image
        shell: bash
        run: |
          echo '${{ secrets.GITHUB_TOKEN }}' | sudo skopeo login --username '${{ github.actor }}' --password-stdin ghcr.io
          version_full=${{ steps.rechunk.outputs.version }}
          version_major=$(echo "$version_full" | cut -d'.' -f1)
          if [[ '${{ github.event_name }}' == 'pull_request' ]]; then
            tags=($version_major.pr-${{ github.event.number }}.build-${{ github.run_number }} $version_major.pr-${{ github.event.number }})
          else
            tags=($version_major.build-${{ github.run_number }} $version_major $version_full latest)
          fi
          for tag in ${tags[@]}; do
            (set -x; sudo skopeo copy --retry-times=2 '${{ steps.rechunk.outputs.ref }}' "docker://ghcr.io/${{ github.repository_owner }}/${{ github.event.repository.name }}:$tag")
          done
